[project]
name = "radon"
version = "0.1.0"
description = "Risk Analysis & Detection Orchestration Node - a self-hosted GCP cloud security posture scanner with local-LLM triage"
requires-python = ">=3.11"
license = "Apache-2.0"
dependencies = [
    "pydantic>=2.0",
    "httpx>=0.27",
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "pymongo>=4.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "mongomock>=4.1",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["radon*", "gcp_emulator*"]

[tool.setuptools.package-data]
radon = ["dashboard/*.html", "dashboard/*.css", "dashboard/*.js"]
gcp_emulator = ["data/*.json"]

[tool.pytest.ini_options]
testpaths = ["test-suite"]
addopts = "-q"
